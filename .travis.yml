language: node_js

sudo: false

script:
  - npm run lint
  - npm run test:coverage

after_script:
  - npm run coveralls

deploy:
  provider: npm
  skip_cleanup: true
  email: ${NPM_EMAIL}
  api_key: ${NPM_AUTH_TOKEN}
  on:
    tags: true
